console.log('edibit-js');
// beyond here is the js I worked on in Edibit.
var arr = ['cat', 'dog', 'duck'];
function getLastItem(arr) {
    return (arr[arr.length - 1]);
 }
 console.log(getLastItem(arr));